---
import PageLayout from "~/layouts/PageLayout.astro";
import SectionRenderer from "~/components/sections/SectionRenderer.astro";

import solutionsDataRaw from "~/data/pages/solutions.json";
import { localizeData } from "~/config/site";
import { buildPageMeta } from "~/lib/pageMeta";
const solutionsData = localizeData(solutionsDataRaw);
const { sections } = solutionsData;

const { metadata, extraSchemas } = buildPageMeta(solutionsData);
---

<PageLayout metadata={metadata} extraSchemas={extraSchemas}>
  <SectionRenderer sections={sections} />
</PageLayout>
